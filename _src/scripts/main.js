import React from 'react'
import ReactDOM from 'react-dom'

import gradimate from './libs/gradimate'

let test = new gradimate(document.getElementById('app'))

// head.appendChild(style);

const render = ReactDOM.render
